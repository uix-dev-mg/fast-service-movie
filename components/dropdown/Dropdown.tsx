'use client'

import React from 'react'

const Dropdown = ({ children }) => {
  return <div>{children}</div>
}

const DropdownTitle = ({ children }) => {
  return <div>{children}</div>
}

const DropdownContent = ({ children }) => {
  return <div>{children}</div>
}
export { Dropdown, DropdownTitle, DropdownContent }
